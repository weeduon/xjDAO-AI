PORT=8787
VITE_API_BASE=http://localhost:8787

# Production notes:
# 1. Do not expose platform API keys in public repositories.
# 2. MVP mode lets users enter their own API keys in the browser settings page.
# 3. For production, encrypt user API keys on the server side and store them in a real database.
